package tools

import (
	"testing"
)

func TestDocker(t *testing.T) {
	//pullImage("busybox")
	//var port = make(map[string]string)
	//port["22"] = "3022"
	//var env = make([]string, 2)
	//env[0] = "ASD=ASD"
	//env[1] = "ASD1=ASD1"
	//createContainer("busybox", "1st", "", env, 2, 1, port, 1)

}
